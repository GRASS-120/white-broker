// import { useParams } from 'react-router-dom';
import OneCreditPage from './OneCreditPage';

const OneCreditPageContainer = () => {
   //const { type, id } = useParams();
   return <OneCreditPage />;
};

export default OneCreditPageContainer;
